class car {
    constructor(color, convertible, speed) {
    this.color = blue;
    this.convertible = false;
    this.speed = 0;
}
   get accelerate(speed) {
       return this.speed + "10";
   } 
    get decelerate(speed) {
        return this.speed - "10";
    }
}

module.exports.car;

